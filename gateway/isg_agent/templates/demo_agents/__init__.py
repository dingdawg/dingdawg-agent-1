"""Demo agent templates for DingDawg Agent 1.

These templates are used to seed live demo agents into the database so that
any visitor to the live site can immediately interact with a fully configured,
realistic business agent — demonstrating the platform's value proposition
without requiring any setup.

Templates are stored as JSON files in this directory and loaded by the seed
scripts in ``isg_agent/scripts/``.
"""
