/**
 * ISG Agent 1 — Messaging Platform Bridges
 *
 * Entry point that re-exports bridge interfaces and types for consumers.
 */

export type { BridgeMessage, BridgeConfig, BridgeStatus } from "./types.js";
